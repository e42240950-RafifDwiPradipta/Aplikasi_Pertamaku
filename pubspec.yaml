name: aplikasi_pertamaku
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.2
  image_picker: ^1.0.4
  firebase_core: ^2.0.0
  cloud_firestore: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# Bagian paling penting untuk Aset/Gambar
flutter:
  uses-material-design: true

  assets:
    - assets/rafif.jpeg
